services:
  jellyclean:
    image: incmve/jellyclean:latest
    container_name: jellyclean
    restart: unless-stopped
    ports:
      - "8099:80"
    environment:
      - TZ=Europe/Amsterdam
    volumes:
      - jellyclean-data:/data

volumes:
  jellyclean-data:
